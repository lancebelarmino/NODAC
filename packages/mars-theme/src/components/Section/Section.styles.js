import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  section: {
    display: 'flex',
    alignItems: 'center',
  },

  container: {
    flexGrow: 1,
  },
}));

export default useStyles;
